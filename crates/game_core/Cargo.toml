[package]
name = "game_core"
version.workspace = true
edition.workspace = true
license.workspace = true

# 关键约束：核心逻辑库**默认零依赖**、不依赖 Bevy。
# 联机需要把部分数据类型序列化，用可选 feature `serde` 开启，默认仍零依赖。
[dependencies]
serde = { workspace = true, optional = true }

[features]
serde = ["dep:serde"]
