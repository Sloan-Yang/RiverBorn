[package]
name = "game_core"
version.workspace = true
edition.workspace = true
license.workspace = true

# 关键约束：核心逻辑库零依赖、不依赖 Bevy。
# 这样下注/战斗规则可以用 `cargo test -p game_core` 秒级验证，
# 之后做联机也能直接复用这套确定性逻辑。
[dependencies]
